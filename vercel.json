{
  "buildCommand": "npm run lint && npm run lint:comms && npm run scan:slop && npm run check:sources -- --strict && npm run check:freshness -- --strict && npm run build",
  "framework": "docusaurus-2",
  "outputDirectory": "build"
}
